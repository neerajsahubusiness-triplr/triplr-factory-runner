on:
  workflow_dispatch:
    inputs:
      hub_limit:
        description: "Number of hubs"
        required: true
        default: "20"

      activity_limit:
        description: "Activities per hub"
        required: true
        default: "5"

      target_tier:
        description: "Tier"
        required: true
        default: "medium"
